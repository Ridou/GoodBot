/**
 * @format
 */

import {AppRegistry} from 'react-native';
import React from 'react';

import App from './App';
import {name as preBot} from './app.json';

AppRegistry.registerComponent(preBot, () => App);
